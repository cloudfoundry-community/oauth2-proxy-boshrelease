<%
require 'digest/sha1'
require 'base64'
%>
<% p('oauth2-proxy.htpasswd', {}).each do |username, password| -%>
<%= username %>:<%= '{SHA}' + Base64.encode64(Digest::SHA1.digest(password)) %>
<% end -%>
