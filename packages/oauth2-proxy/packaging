#!/usr/bin/env bash
set -euo pipefail

# Copy common utils
mkdir -p "${BOSH_INSTALL_TARGET}/common"
cp "${BOSH_COMPILE_TARGET}/common/utils.sh" "${BOSH_INSTALL_TARGET}/common"

# Extract oauth2-proxy package
mkdir -p "${BOSH_INSTALL_TARGET}/bin"
tar xzf oauth2-proxy/oauth2_proxy-v3.2.0.linux-amd64.go1.11.tar.gz
cp "${BOSH_COMPILE_TARGET}/release/oauth2_proxy-linux-amd64" "${BOSH_INSTALL_TARGET}/bin/oauth2-proxy"
chmod 755 "${BOSH_INSTALL_TARGET}/bin/oauth2-proxy"
